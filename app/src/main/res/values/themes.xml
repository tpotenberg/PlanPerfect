<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PlanPerfect" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryDark">@color/transparent</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondary_color</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="31">@color/secondary_color</item>
        <!-- Customize your theme here. -->
    </style>

        <!-- Date picker theme -->
    <style name="CustomDatePickerStyle" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:datePickerMode">spinner</item> <!-- Use 'calendar' or 'spinner' mode -->
        <item name="android:background">@color/midnight</item>
        <item name="android:textColorPrimary">@color/jade</item>
        <item name="android:textColorSecondary">@color/mango</item>
    </style>

    <style name="Theme.PlanPerfect" parent="Base.Theme.PlanPerfect"/>
</resources>